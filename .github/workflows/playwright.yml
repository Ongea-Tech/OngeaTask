name: Playwright Tests

on:
  push:
    branches: [main, Kawira/playwright]
  pull_request:
    branches: [main]

jobs:
  test:
    runs-on: ubuntu-latest
    timeout-minutes: 60

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install Python dependencies
        run: |
          python -m venv venv
          source venv/bin/activate
          pip install -r requirements.txt

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Install Playwright
        run: npm ci

      - name: Install Playwright browsers
        run: npx playwright install --with-deps

      - name: Start Flask and run tests
        run: |
          source venv/bin/activate
          flask run &
          sleep 5
          npx playwright test
        env:
          FLASK_APP: app
          FLASK_ENV: development
          APP_EMAIL: ${{ secrets.Kawira }}
          APP_PASSWORD: ${{ secrets.@Ongeatask09 }}
          SECRET_KEY: ${{ secrets.secret-for-wtf-csrf-protection }}
          MAIL_PORT: ${{ secrets.587 }}
          MAIL_SERVER: ${{ secrets.smtp.gmail.com }}
          MAIL_USERNAME: ${{ secrets.tarus2659@gmail.com }}
          MAIL_PASSWORD: ${{ secrets.mddrwbxsyqvfokux }}
      - name: Upload Playwright report
        uses: actions/upload-artifact@v4
        if: always()
        with:
          name: playwright-report
          path: playwright-report/
          retention-days: 30
